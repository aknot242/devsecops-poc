---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-ingress
spec:
  template:
    spec:
      containers:
        - name: nginx-plus-ingress
          args:
            - -nginx-plus
            - -enable-snippets
            - -nginx-configmaps=devsecops-prod/nginx-config
            - -default-server-tls-secret=devsecops-prod/default-server-secret
            - -enable-app-protect
            - -enable-app-protect-dos
            - -watch-namespace=devsecops-prod
            - -ingress-class=nginx-prod
