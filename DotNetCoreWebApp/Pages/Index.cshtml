@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

<div class="text-center">
    <h1 class="display-4">Welcome to DevSecOps at the NGINX Sprint 2.0 conference</h1>
    <p>This is a .NET Core application container running in Azure Kubernetes Service.</p>
</div>

<div class="text-center">
    <form>
        <input asp-for="SearchString" />
        <input type="submit" value="Search" />
    </form>
</div>
@* @if (!string.IsNullOrEmpty(@Model.SearchString))
{
    <div class="text-center">Search result: @Html.Raw(@Model.SearchString)</div>
} *@
